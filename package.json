{
  "name": "artery_project",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start --workspace server",
    "dev": "npm run dev --workspace client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.2"
  }
}
